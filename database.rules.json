{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "rooms": {
      ".read": "auth != null",
      "$room_id": {
        ".write": "auth != null && ((!data.exists() && newData.child('ownerUid').val() === auth.uid) || (data.exists() && (data.child('ownerUid').val() === auth.uid || newData.child('playerUids').child(auth.uid).exists() || root.child('users').child(auth.uid).child('isAdmin').val() === true)))"
      }
    },
    "games": {
      "$game_id": {
        ".read": "auth != null",
        ".write": "auth != null && ((!data.exists() && newData.child('ownerUid').val() === auth.uid) || (data.exists() && (data.child('ownerUid').val() === auth.uid || data.child('playerUids').child(auth.uid).exists() || newData.child('playerUids').child(auth.uid).exists() || root.child('users').child(auth.uid).child('isAdmin').val() === true)))"
      }
    }
  }
}
